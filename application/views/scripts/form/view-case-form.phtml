<fieldset>
    <legend>Case summary:</legend>
    <div class=search-results>
        <table class=search-results>
            <?php
            echo $this->partial('partial/searchResultCase.phtml', array(
                'case' => $this->case,
                'noCaseLinks' => true,
            ));
            ?>
        </table>
    </div>
    <?php if ($this->case->getStatus() !== 'Closed') { ?>
    <div class=control-group>
        <div class=controls>
            <?php echo $this->element->closeCase; ?>
        </div>
    </div>
    <?php } ?>
</fieldset>

<fieldset>
    <legend>Case needs:</legend>

    <?php
    $this->partialLoop()->setObjectKey('need');
    echo $this->partialLoop('partial/case-need.phtml', $this->case->getNeeds());
    ?>
</fieldset>

<?php echo $this->element->visitRecordList; ?>

<?php echo $this->element->commentsSubForm; ?>
